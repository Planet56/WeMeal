package com.wemeal.app

import android.app.Application

class WeMealApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize things if needed (e.g. Timber, Firebase if we had it)
    }
}
